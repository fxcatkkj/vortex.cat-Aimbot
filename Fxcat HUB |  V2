local mt=getrawmetatable(game)
local oldNamecall=mt.__namecall
local oldIndex=mt.__index
local oldNewIndex=mt.__newindex
setreadonly(mt,false)

mt.__namecall=newcclosure(function(Self,...)
    local Args={...}
    local Method=getnamecallmethod()
    local blocked={"FireServer","InvokeServer","kick","ban","report","flag","detect","log","HttpService","TeleportService","ReplicatedStorage"}
    for _,block in pairs(blocked)do
        if Method:lower():find(block:lower())or tostring(Self):lower():find(block:lower())then
            return wait(math.huge)
        end
    end
    return oldNamecall(Self,...)
end)

mt.__index=newcclosure(function(Self,Key)
    return oldIndex(Self,Key)
end)

mt.__newindex=newcclosure(function(Self,Key,Value)
    return oldNewIndex(Self,Key,Value)
end)

setreadonly(mt,true)

local getrenv=getrenv()
local oldspawn=getrenv().spawn or getrenv()._G.spawn
getrenv().spawn=function(func)
    return oldspawn(function()
        local ok,err=pcall(func)
        if not ok then return end
    end)
end

local rs=game:GetService("RunService")
rs.Heartbeat:Connect(function()end)

local function cloakObject(obj)
    obj.Name="CrSoAkEd"
    obj.Parent=nil
    obj.Parent=workspace
end

game:GetService("ReplicatedStorage").OnClientEvent:Connect(function()end)

local Sirius,Core=loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Window=Core:CreateWindow({
    Name="Fxcat HUB | Undetected",
    LoadingTitle="Loading",
    LoadingSubtitle="by fxcatkkj",
    ConfigurationSaving={
        Enabled=true,
        FolderName="FxcatHUB",
        FileName="undetected"
    },
    KeySystem=false
})

local Players=game:GetService("Players")
local UserInputService=game:GetService("UserInputService")
local RunService=game:GetService("RunService")
local Workspace=game:GetService("Workspace")
local Camera=Workspace.CurrentCamera
local LocalPlayer=Players.LocalPlayer

local Features={Aimbot=false,FlingPlayer=false,FootFly=false,CarFly=false,VehicleFling=false}
local Settings={AimbotDistance=1500,AimbotMaxTargets=3,AimbotSmoothness=0.08,AimbotFOV=150,FootFlySpeed=75,CarFlySpeed=150,ShowFOV=false}

local CombatTab=Window:CreateTab("Combat",4483362458)
local MovementTab=Window:CreateTab("Movement",4483362458)

CombatTab:CreateToggle({Name="Aimbot",CurrentValue=false,Callback=function(Value)Features.Aimbot=Value end})
CombatTab:CreateSlider({Name="Distance",Range={50,3000},Increment=50,Suffix=" studs",CurrentValue=1500,Callback=function(Value)Settings.AimbotDistance=Value end})
CombatTab:CreateSlider({Name="Max Targets",Range={1,10},Increment=1,Suffix=" players",CurrentValue=3,Callback=function(Value)Settings.AimbotMaxTargets=Value end})
CombatTab:CreateSlider({Name="Smoothness",Range={1,30},Increment=1,Suffix="% smoother",CurrentValue=8,Callback=function(Value)Settings.AimbotSmoothness=Value/100 end})
CombatTab:CreateSlider({Name="FOV",Range={30,500},Increment=10,Suffix="°",CurrentValue=150,Callback=function(Value)Settings.AimbotFOV=Value end})
CombatTab:CreateToggle({Name="Show FOV",CurrentValue=false,Callback=function(Value)Settings.ShowFOV=Value end})

CombatTab:CreateToggle({Name="Fling Player",CurrentValue=false,Callback=function(Value)Features.FlingPlayer=Value end})
CombatTab:CreateToggle({Name="Vehicle Fling",CurrentValue=false,Callback=function(Value)Features.VehicleFling=Value end})

MovementTab:CreateToggle({Name="Foot Fly",CurrentValue=false,Callback=function(Value)Features.FootFly=Value end})
MovementTab:CreateSlider({Name="Foot Speed",Range={16,250},Increment=5,Suffix=" speed",CurrentValue=75,Callback=function(Value)Settings.FootFlySpeed=Value end})
MovementTab:CreateToggle({Name="Car Fly",CurrentValue=false,Callback=function(Value)Features.CarFly=Value end})
MovementTab:CreateSlider({Name="Car Speed",Range={50,600},Increment=10,Suffix=" speed",CurrentValue=150,Callback=function(Value)Settings.CarFlySpeed=Value end})

local FOVCircle=Drawing.new("Circle")
FOVCircle.Thickness=3
FOVCircle.NumSides=128
FOVCircle.Color=Color3.fromRGB(255,40,40)
FOVCircle.Filled=false
FOVCircle.Transparency=0.7
FOVCircle.Radius=Settings.AimbotFOV

local function GetClosestTarget()
    local Closest,Distance=nil,Settings.AimbotDistance
    local TargetCount=0
    for _,Player in pairs(Players:GetPlayers())do
        if Player~=LocalPlayer and Player.Character and Player.Character:FindFirstChild("Head")then
            local Head=Player.Character.Head
            local ScreenPos,OnScreen=Camera:WorldToViewportPoint(Head.Position)
            local MousePos=Vector2.new(Camera.ViewportSize.X/2,Camera.ViewportSize.Y/2)
            local Dist=(Vector2.new(ScreenPos.X,ScreenPos.Y)-MousePos).Magnitude
            if OnScreen and Dist<Settings.AimbotFOV and(Head.Position-Camera.CFrame.Position).Magnitude<Settings.AimbotDistance then
                TargetCount=TargetCount+1
                if TargetCount<=Settings.AimbotMaxTargets then
                    Closest=Player
                    Distance=Dist
                end
            end
        end
    end
    return Closest
end

RunService.RenderStepped:Connect(function()
    if Settings.ShowFOV then
        FOVCircle.Visible=true
        FOVCircle.Radius=Settings.AimbotFOV
        FOVCircle.Position=UserInputService:GetMouseLocation()
    else
        FOVCircle.Visible=false
    end
    if Features.Aimbot then
        local Target=GetClosestTarget()
        if Target and Target.Character and Target.Character:FindFirstChild("Head")then
            local TargetPos=Target.Character.Head.Position
            local CurrentCFrame=Camera.CFrame
            Camera.CFrame=CurrentCFrame:Lerp(CFrame.lookAt(CurrentCFrame.Position,TargetPos),Settings.AimbotSmoothness)
        end
    end
end)

local function CreateFling(part)
    if not part or part.Parent==LocalPlayer.Character then return end
    local BodyVelocity=Instance.new("BodyVelocity")
    BodyVelocity.MaxForce=Vector3.new(40000,40000,40000)
    BodyVelocity.Velocity=Vector3.new(math.random(-25000,25000),math.random(35000,55000),math.random(-25000,25000))
    BodyVelocity.Parent=part
    game:GetService("Debris"):AddItem(BodyVelocity,0.25)
end

if LocalPlayer.Character then
    LocalPlayer.Character.DescendantAdded:Connect(function(descendant)
        if Features.FlingPlayer and descendant:IsA("BasePart")then
            descendant.Touched:Connect(function(hit)
                if hit.Parent==LocalPlayer.Character then return end
                task.spawn(function()
                    wait(0.01)
                    CreateFling(descendant)
                end)
            end)
        end
    end)
end

RunService.Heartbeat:Connect(function()
    local Character=LocalPlayer.Character
    if not Character then return end
    local RootPart=Character:FindFirstChild("HumanoidRootPart")
    if not RootPart then return end
    local CameraDirection=Workspace.CurrentCamera.CFrame
    if Features.FootFly then
        local FlyBV=RootPart:FindFirstChild("FxcatFlyBV")
        if not FlyBV then
            FlyBV=Instance.new("BodyVelocity")
            FlyBV.Name="FxcatFlyBV"
            FlyBV.MaxForce=Vector3.new(40000,40000,40000)
            FlyBV.Parent=RootPart
        end
        local MoveVector=Vector3.new((UserInputService:IsKeyDown(Enum.KeyCode.D)and 1 or 0)-(UserInputService:IsKeyDown(Enum.KeyCode.A)and 1 or 0),(UserInputService:IsKeyDown(Enum.KeyCode.Space)and 1 or 0)-(UserInputService:IsKeyDown(Enum.KeyCode.LeftShift)and 1 or 0),(UserInputService:IsKeyDown(Enum.KeyCode.S)and 1 or 0)-(UserInputService:IsKeyDown(Enum.KeyCode.W)and 1 or 0))*Settings.FootFlySpeed
        FlyBV.Velocity=CameraDirection:VectorToWorldSpace(MoveVector)
    else
        local FlyBV=RootPart:FindFirstChild("FxcatFlyBV")
        if FlyBV then FlyBV:Destroy()end
    end
    local Seat=Character:FindFirstChildOfClass("SeatWeld")or Character:FindFirstChildOfClass("VehicleSeat")
    if Seat then
        local Vehicle=Seat.Parent
        local VehicleRoot=Vehicle.PrimaryPart or Vehicle:FindFirstChild("HumanoidRootPart")
        if VehicleRoot then
            if Features.VehicleFling then
                for _,Part in pairs(Workspace:GetPartBoundsInRadius(VehicleRoot.Position,30))do
                    if Part.Parent~=Vehicle and Part.Parent~=Character then
                        CreateFling(Part)
                    end
                end
            end
            if Features.CarFly then
                local CarBV=VehicleRoot:FindFirstChild("FxcatCarFly")
                if not CarBV then
                    CarBV=Instance.new("BodyVelocity")
                    CarBV.Name="FxcatCarFly"
                    CarBV.MaxForce=Vector3.new(50000,50000,50000)
                    CarBV.Parent=VehicleRoot
                end
                local CarMoveVector=Vector3.new((UserInputService:IsKeyDown(Enum.KeyCode.D)and 1 or 0)-(UserInputService:IsKeyDown(Enum.KeyCode.A)and 1 or 0),(UserInputService:IsKeyDown(Enum.KeyCode.Space)and 1 or 0)-(UserInputService:IsKeyDown(Enum.KeyCode.LeftShift)and 1 or 0),(UserInputService:IsKeyDown(Enum.KeyCode.S)and 1 or 0)-(UserInputService:IsKeyDown(Enum.KeyCode.W)and 1 or 0))*Settings.CarFlySpeed
                CarBV.Velocity=CameraDirection:VectorToWorldSpace(CarMoveVector)
            else
                local CarBV=VehicleRoot:FindFirstChild("FxcatCarFly")
                if CarBV then CarBV:Destroy()end
            end
        end
    end
end)

LocalPlayer.CharacterAdded:Connect(function()
    wait(2)
end)

UserInputService.InputBegan:Connect(function(input)
    if input.KeyCode==Enum.KeyCode.Home then
        Window:Toggle()
    end
end)
